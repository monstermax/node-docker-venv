#!/bin/sh
# Opens an interactive bash shell inside the container as root.

[ -z "$VENV_CONTAINER" ] && echo "Error: VENV_CONTAINER is not set (is the venv active?)" >&2 && exit 1

exec docker exec -u root -it \
  -w "$PWD" \
  "$VENV_CONTAINER" bash "$@"
