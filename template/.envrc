

# List of ports to map to localhost, separated by ,
#export VENV_PORTS=3000,5173



# Note: after editing this file, run `direnv allow` to approve its content

# In some cases you may also need to restart the Docker container.
# Just stop it with `.envrc.sandbox/docker/stop.sh` and it will restart automatically on demand.


# Activate the venv
source .envrc.sandbox/activate

